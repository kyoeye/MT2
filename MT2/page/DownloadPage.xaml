<Page
    x:Class="MT2.page.DownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MT2.page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="#FF0074B6"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions >
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Pivot Name="pivot" >
            <PivotItem Header="下载列表"  Margin="-0,0,-0,0"  >
                <Grid >
                    <ListView >
                        <ListView.ItemTemplate >
                            <DataTemplate >
                                <Grid >
                                    <ProgressBar Name="Downloadprogress" Height="100" />
                                    <TextBlock FontSize="30" Name="imgid" HorizontalAlignment="Left"  />
                                    <StackPanel HorizontalAlignment="Right" >
                                        <AppBarButton Name="Zanting" Height="100" Width="50" >
                                            <TextBlock Text="&#xE769;" FontFamily="Segoe MDL2 Assets" />
                                        </AppBarButton>
                                        <AppBarButton Name="Quxiao" Height="100" Width="50" >
                                            <TextBlock Text="&#xE106;" FontFamily="Segoe MDL2 Assets" />
                                        </AppBarButton>

                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
            <PivotItem Margin="0" Header="下载历史">
                <TextBlock  Text="1"/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
